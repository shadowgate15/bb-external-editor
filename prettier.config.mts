import { type Config } from 'prettier'

const config: Config = {
  printWidth: 120,
  trailingComma: 'all',
  singleQuote: true,
  semi: false,
  useTabs: false,
  tabWidth: 2,
}

export default config
